apiVersion: v1
kind: Service
metadata:
    name: anishpaladicapstonebookstore
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: anishpaladicapstonebookstore